<!-- app/templates/authors.hbs -->
<h1>Books</h1>

<table class="table table-bordered table-striped">
  <thead>
  <tr>
    <th>
      Name
      <br><small class="small not-bold">(Click on name for editing)</small>
    </th>
    <th class="vtop">Books</th>
  </tr>
  </thead>
  <tbody>
  {{#each model as |book|}}
    <tr>
      <td>
        {{#if book.isEditing}}
          <form {{action 'saveAuthor' book on='submit'}} class="form-inline">
            <div class="input-group">
              {{input value=book.name class="form-control"}}
              <div class="input-group-btn">
                <button type="submit" class="btn btn-success" disabled={{book.isNotValid}}>Save</button>
                <button class="btn btn-danger" {{action 'cancelAuthorEdit' book}}>Cancel</button>
              </div>
            </div>
          </form>
        {{else}}
          <span {{action 'editAuthor' book}}>{{book.title}}</span>
        {{/if}}
      </td>
      <td>{{book.author.name}}
        <ul>
          {{#each book.authors as |author|}}
            <li>***{{author.name}}</li>
          {{/each}}
        </ul>
      </td>
    </tr>
  {{/each}}
  </tbody>
</table> 